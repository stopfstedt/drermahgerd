default:
  extensions:
    Behat\MinkExtension\Extension:
      base_url: http://drermahgerd.local
      default_session: goutte
      javascript_session: selenium2
      browser_name: "firefox"
      goutte: ~
      selenium2:
        capabilities: { "browser": "firefox", "version": "26" }
    Drupal\DrupalExtension\Extension:
      blackbox: ~
      api_driver: "drupal"
      drush:
        alias: "drermahgerd"
      drupal:
        drupal_root: "/var/www/sites/drermahgerd"

browser:
  extensions:
    Behat\MinkExtension\Extension:
      default_session: selenium2

travis:
    extensions:
      Behat\MinkExtension\Extension:
        base_url: http://127.0.0.1:8080
      Drupal\DrupalExtension\Extension:
        drupal:
          # this is hokey, find a better way of defining the build dir [ST 2014/02/22]
          drupal_root: "/home/travis/build/travis_ci_drermahgerd_test/drupal"

